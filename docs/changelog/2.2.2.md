- Add unit testing to release flow.
- Add editable dependency handling with version pinning.
- Allow editable dependency pinning with extras.
- Allow pre-release projects to pin pre-release editable dependencies.
- Simplify infra and expand unit test coverage for editable dependency pinning.
- Fix test execution order (tests now run after version increment logic is applied correctly).
